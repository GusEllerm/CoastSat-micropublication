```{python exec}
# This code makes the micropublication aware of its interface data.
import pandas as pd
import os
# Load the interface data
data_path = os.path.join(os.getcwl(), "data.csv")
df = pd.read_csv(data_path) 
```



**Example Title**

```python exec
test = "example_code"
set = [1, 2, 3]
print(test)
```

## Example subsection

1. Example item
2. Second item
3. Third item

Example inline variable dereference: `test`{python exec}

::: for item in `set`{python exec}

- {{ item }}

:::